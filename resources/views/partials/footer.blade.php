{{-- <footer>
    <div class="footerLinks">
        <div class="container">
          <footer>
            <div class="links">
              <ul
              v-for="(el, index) in links" :key="`links-${index}`">
                <li>
                  <h3>{{el.linkTitle}}</h3>
                </li>
                <li
                v-for="(li,index) in el.link" :key="`links-${index}`">
                  <a :href="li.href">{{li.txt}}</a>
                  </li>
              </ul>
            </div>
            <img src="../assets/img/dc-logo-bg.png" alt="">
          </footer>
        </div>
      </div>
</footer> --}}
